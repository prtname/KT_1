<UserControl x:Class="KT_1.View.UserControls.NavigationBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KT_1.View.UserControls"
             mc:Ignorable="d"
             Name="uc">
    <ItemsControl ItemsSource="{Binding ElementName=uc, Path=Items}">
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <StackPanel>
                    <RadioButton Content="{Binding Title}" Command="{Binding ClickCommand}" Tag="{Binding PageUri}" Checked="RadioButton_Checked"></RadioButton>
                </StackPanel>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
